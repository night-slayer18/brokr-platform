package io.brokr.core.model;

public enum AuditResourceType {
    USER,
    ORGANIZATION,
    ENVIRONMENT,
    CLUSTER,
    TOPIC,
    CONSUMER_GROUP,
    SCHEMA_REGISTRY,
    KAFKA_CONNECT,
    KAFKA_STREAMS,
    KSQLDB,
    MESSAGE,
    MESSAGE_REPLAY,
    SCHEMA,
    CONNECTOR
}

